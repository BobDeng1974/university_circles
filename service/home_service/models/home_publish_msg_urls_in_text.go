// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package home

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// HomeUrlsInText is an object representing the database table.
type HomeUrlsInText struct {
	ID          uint      `boil:"id" json:"id" toml:"id" yaml:"id"`
	OwnerID     string    `boil:"owner_id" json:"owner_id" toml:"owner_id" yaml:"owner_id"`
	Title       string    `boil:"title" json:"title" toml:"title" yaml:"title"`
	OriginalUrl string    `boil:"originalUrl" json:"originalUrl" toml:"originalUrl" yaml:"originalUrl"`
	URL         string    `boil:"url" json:"url" toml:"url" yaml:"url"`
	CreatedAt   time.Time `boil:"createdAt" json:"createdAt" toml:"createdAt" yaml:"createdAt"`

	R *homeUrlsInTextR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L homeUrlsInTextL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var HomeUrlsInTextColumns = struct {
	ID          string
	OwnerID     string
	Title       string
	OriginalUrl string
	URL         string
	CreatedAt   string
}{
	ID:          "id",
	OwnerID:     "owner_id",
	Title:       "title",
	OriginalUrl: "originalUrl",
	URL:         "url",
	CreatedAt:   "createdAt",
}

// Generated where

var HomeUrlsInTextWhere = struct {
	ID          whereHelperuint
	OwnerID     whereHelperstring
	Title       whereHelperstring
	OriginalUrl whereHelperstring
	URL         whereHelperstring
	CreatedAt   whereHelpertime_Time
}{
	ID:          whereHelperuint{field: "`home_publish_msg_urls_in_text`.`id`"},
	OwnerID:     whereHelperstring{field: "`home_publish_msg_urls_in_text`.`owner_id`"},
	Title:       whereHelperstring{field: "`home_publish_msg_urls_in_text`.`title`"},
	OriginalUrl: whereHelperstring{field: "`home_publish_msg_urls_in_text`.`originalUrl`"},
	URL:         whereHelperstring{field: "`home_publish_msg_urls_in_text`.`url`"},
	CreatedAt:   whereHelpertime_Time{field: "`home_publish_msg_urls_in_text`.`createdAt`"},
}

// HomeUrlsInTextRels is where relationship names are stored.
var HomeUrlsInTextRels = struct {
}{}

// homeUrlsInTextR is where relationships are stored.
type homeUrlsInTextR struct {
}

// NewStruct creates a new relationship struct
func (*homeUrlsInTextR) NewStruct() *homeUrlsInTextR {
	return &homeUrlsInTextR{}
}

// homeUrlsInTextL is where Load methods for each relationship are stored.
type homeUrlsInTextL struct{}

var (
	homeUrlsInTextAllColumns            = []string{"id", "owner_id", "title", "originalUrl", "url", "createdAt"}
	homeUrlsInTextColumnsWithoutDefault = []string{"owner_id", "title", "originalUrl", "url"}
	homeUrlsInTextColumnsWithDefault    = []string{"id", "createdAt"}
	homeUrlsInTextPrimaryKeyColumns     = []string{"id"}
)

type (
	// HomeUrlsInTextSlice is an alias for a slice of pointers to HomeUrlsInText.
	// This should generally be used opposed to []HomeUrlsInText.
	HomeUrlsInTextSlice []*HomeUrlsInText
	// HomeUrlsInTextHook is the signature for custom HomeUrlsInText hook methods
	HomeUrlsInTextHook func(context.Context, boil.ContextExecutor, *HomeUrlsInText) error

	homeUrlsInTextQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	homeUrlsInTextType                 = reflect.TypeOf(&HomeUrlsInText{})
	homeUrlsInTextMapping              = queries.MakeStructMapping(homeUrlsInTextType)
	homeUrlsInTextPrimaryKeyMapping, _ = queries.BindMapping(homeUrlsInTextType, homeUrlsInTextMapping, homeUrlsInTextPrimaryKeyColumns)
	homeUrlsInTextInsertCacheMut       sync.RWMutex
	homeUrlsInTextInsertCache          = make(map[string]insertCache)
	homeUrlsInTextUpdateCacheMut       sync.RWMutex
	homeUrlsInTextUpdateCache          = make(map[string]updateCache)
	homeUrlsInTextUpsertCacheMut       sync.RWMutex
	homeUrlsInTextUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var homeUrlsInTextBeforeInsertHooks []HomeUrlsInTextHook
var homeUrlsInTextBeforeUpdateHooks []HomeUrlsInTextHook
var homeUrlsInTextBeforeDeleteHooks []HomeUrlsInTextHook
var homeUrlsInTextBeforeUpsertHooks []HomeUrlsInTextHook

var homeUrlsInTextAfterInsertHooks []HomeUrlsInTextHook
var homeUrlsInTextAfterSelectHooks []HomeUrlsInTextHook
var homeUrlsInTextAfterUpdateHooks []HomeUrlsInTextHook
var homeUrlsInTextAfterDeleteHooks []HomeUrlsInTextHook
var homeUrlsInTextAfterUpsertHooks []HomeUrlsInTextHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *HomeUrlsInText) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range homeUrlsInTextBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *HomeUrlsInText) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range homeUrlsInTextBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *HomeUrlsInText) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range homeUrlsInTextBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *HomeUrlsInText) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range homeUrlsInTextBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *HomeUrlsInText) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range homeUrlsInTextAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *HomeUrlsInText) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range homeUrlsInTextAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *HomeUrlsInText) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range homeUrlsInTextAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *HomeUrlsInText) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range homeUrlsInTextAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *HomeUrlsInText) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range homeUrlsInTextAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddHomeUrlsInTextHook registers your hook function for all future operations.
func AddHomeUrlsInTextHook(hookPoint boil.HookPoint, homeUrlsInTextHook HomeUrlsInTextHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		homeUrlsInTextBeforeInsertHooks = append(homeUrlsInTextBeforeInsertHooks, homeUrlsInTextHook)
	case boil.BeforeUpdateHook:
		homeUrlsInTextBeforeUpdateHooks = append(homeUrlsInTextBeforeUpdateHooks, homeUrlsInTextHook)
	case boil.BeforeDeleteHook:
		homeUrlsInTextBeforeDeleteHooks = append(homeUrlsInTextBeforeDeleteHooks, homeUrlsInTextHook)
	case boil.BeforeUpsertHook:
		homeUrlsInTextBeforeUpsertHooks = append(homeUrlsInTextBeforeUpsertHooks, homeUrlsInTextHook)
	case boil.AfterInsertHook:
		homeUrlsInTextAfterInsertHooks = append(homeUrlsInTextAfterInsertHooks, homeUrlsInTextHook)
	case boil.AfterSelectHook:
		homeUrlsInTextAfterSelectHooks = append(homeUrlsInTextAfterSelectHooks, homeUrlsInTextHook)
	case boil.AfterUpdateHook:
		homeUrlsInTextAfterUpdateHooks = append(homeUrlsInTextAfterUpdateHooks, homeUrlsInTextHook)
	case boil.AfterDeleteHook:
		homeUrlsInTextAfterDeleteHooks = append(homeUrlsInTextAfterDeleteHooks, homeUrlsInTextHook)
	case boil.AfterUpsertHook:
		homeUrlsInTextAfterUpsertHooks = append(homeUrlsInTextAfterUpsertHooks, homeUrlsInTextHook)
	}
}

// One returns a single homeUrlsInText record from the query.
func (q homeUrlsInTextQuery) One(ctx context.Context, exec boil.ContextExecutor) (*HomeUrlsInText, error) {
	o := &HomeUrlsInText{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "home: failed to execute a one query for home_publish_msg_urls_in_text")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all HomeUrlsInText records from the query.
func (q homeUrlsInTextQuery) All(ctx context.Context, exec boil.ContextExecutor) (HomeUrlsInTextSlice, error) {
	var o []*HomeUrlsInText

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "home: failed to assign all query results to HomeUrlsInText slice")
	}

	if len(homeUrlsInTextAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all HomeUrlsInText records in the query.
func (q homeUrlsInTextQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "home: failed to count home_publish_msg_urls_in_text rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q homeUrlsInTextQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "home: failed to check if home_publish_msg_urls_in_text exists")
	}

	return count > 0, nil
}

// HomeUrlsInTexts retrieves all the records using an executor.
func HomeUrlsInTexts(mods ...qm.QueryMod) homeUrlsInTextQuery {
	mods = append(mods, qm.From("`home_publish_msg_urls_in_text`"))
	return homeUrlsInTextQuery{NewQuery(mods...)}
}

// FindHomeUrlsInText retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindHomeUrlsInText(ctx context.Context, exec boil.ContextExecutor, iD uint, selectCols ...string) (*HomeUrlsInText, error) {
	homeUrlsInTextObj := &HomeUrlsInText{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `home_publish_msg_urls_in_text` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, homeUrlsInTextObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "home: unable to select from home_publish_msg_urls_in_text")
	}

	return homeUrlsInTextObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *HomeUrlsInText) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("home: no home_publish_msg_urls_in_text provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(homeUrlsInTextColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	homeUrlsInTextInsertCacheMut.RLock()
	cache, cached := homeUrlsInTextInsertCache[key]
	homeUrlsInTextInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			homeUrlsInTextAllColumns,
			homeUrlsInTextColumnsWithDefault,
			homeUrlsInTextColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(homeUrlsInTextType, homeUrlsInTextMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(homeUrlsInTextType, homeUrlsInTextMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `home_publish_msg_urls_in_text` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `home_publish_msg_urls_in_text` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `home_publish_msg_urls_in_text` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, homeUrlsInTextPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "home: unable to insert into home_publish_msg_urls_in_text")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == homeUrlsInTextMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "home: unable to populate default values for home_publish_msg_urls_in_text")
	}

CacheNoHooks:
	if !cached {
		homeUrlsInTextInsertCacheMut.Lock()
		homeUrlsInTextInsertCache[key] = cache
		homeUrlsInTextInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the HomeUrlsInText.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *HomeUrlsInText) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	homeUrlsInTextUpdateCacheMut.RLock()
	cache, cached := homeUrlsInTextUpdateCache[key]
	homeUrlsInTextUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			homeUrlsInTextAllColumns,
			homeUrlsInTextPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("home: unable to update home_publish_msg_urls_in_text, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `home_publish_msg_urls_in_text` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, homeUrlsInTextPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(homeUrlsInTextType, homeUrlsInTextMapping, append(wl, homeUrlsInTextPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "home: unable to update home_publish_msg_urls_in_text row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "home: failed to get rows affected by update for home_publish_msg_urls_in_text")
	}

	if !cached {
		homeUrlsInTextUpdateCacheMut.Lock()
		homeUrlsInTextUpdateCache[key] = cache
		homeUrlsInTextUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q homeUrlsInTextQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "home: unable to update all for home_publish_msg_urls_in_text")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "home: unable to retrieve rows affected for home_publish_msg_urls_in_text")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o HomeUrlsInTextSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("home: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), homeUrlsInTextPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `home_publish_msg_urls_in_text` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, homeUrlsInTextPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "home: unable to update all in homeUrlsInText slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "home: unable to retrieve rows affected all in update all homeUrlsInText")
	}
	return rowsAff, nil
}

var mySQLHomeUrlsInTextUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *HomeUrlsInText) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("home: no home_publish_msg_urls_in_text provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(homeUrlsInTextColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLHomeUrlsInTextUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	homeUrlsInTextUpsertCacheMut.RLock()
	cache, cached := homeUrlsInTextUpsertCache[key]
	homeUrlsInTextUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			homeUrlsInTextAllColumns,
			homeUrlsInTextColumnsWithDefault,
			homeUrlsInTextColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			homeUrlsInTextAllColumns,
			homeUrlsInTextPrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("home: unable to upsert home_publish_msg_urls_in_text, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "home_publish_msg_urls_in_text", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `home_publish_msg_urls_in_text` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(homeUrlsInTextType, homeUrlsInTextMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(homeUrlsInTextType, homeUrlsInTextMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "home: unable to upsert for home_publish_msg_urls_in_text")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == homeUrlsInTextMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(homeUrlsInTextType, homeUrlsInTextMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "home: unable to retrieve unique values for home_publish_msg_urls_in_text")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, nzUniqueCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "home: unable to populate default values for home_publish_msg_urls_in_text")
	}

CacheNoHooks:
	if !cached {
		homeUrlsInTextUpsertCacheMut.Lock()
		homeUrlsInTextUpsertCache[key] = cache
		homeUrlsInTextUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single HomeUrlsInText record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *HomeUrlsInText) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("home: no HomeUrlsInText provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), homeUrlsInTextPrimaryKeyMapping)
	sql := "DELETE FROM `home_publish_msg_urls_in_text` WHERE `id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "home: unable to delete from home_publish_msg_urls_in_text")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "home: failed to get rows affected by delete for home_publish_msg_urls_in_text")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q homeUrlsInTextQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("home: no homeUrlsInTextQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "home: unable to delete all from home_publish_msg_urls_in_text")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "home: failed to get rows affected by deleteall for home_publish_msg_urls_in_text")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o HomeUrlsInTextSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(homeUrlsInTextBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), homeUrlsInTextPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `home_publish_msg_urls_in_text` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, homeUrlsInTextPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "home: unable to delete all from homeUrlsInText slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "home: failed to get rows affected by deleteall for home_publish_msg_urls_in_text")
	}

	if len(homeUrlsInTextAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *HomeUrlsInText) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindHomeUrlsInText(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *HomeUrlsInTextSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := HomeUrlsInTextSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), homeUrlsInTextPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `home_publish_msg_urls_in_text`.* FROM `home_publish_msg_urls_in_text` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, homeUrlsInTextPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "home: unable to reload all in HomeUrlsInTextSlice")
	}

	*o = slice

	return nil
}

// HomeUrlsInTextExists checks if the HomeUrlsInText row exists.
func HomeUrlsInTextExists(ctx context.Context, exec boil.ContextExecutor, iD uint) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `home_publish_msg_urls_in_text` where `id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}

	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "home: unable to check if home_publish_msg_urls_in_text exists")
	}

	return exists, nil
}
